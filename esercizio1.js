// Definisci un valore predefinito per il numero di volte
const numeroDiVolte = 10;

// Ciclo per stampare il messaggio il numero specificato di volte
for (let i = 1; i <= numeroDiVolte; i++) {
    console.log(`Messaggio ${i}`);
}

    
